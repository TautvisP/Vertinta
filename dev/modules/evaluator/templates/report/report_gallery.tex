\pgfsetlayers{background,main,foreground}

\section{Galerija}
\begin{itemize}
    {% for image in images %}
    \item
    \begin{figure}[htbp]
        \centering
        \begin{tikzpicture}
            \begin{pgfonlayer}{background}
                \node[inner sep=0pt, anchor=north west] (image) at (0,0) {\includegraphics[width=0.8\textwidth]{ {{ image.image.path|escape }} }};
            \end{pgfonlayer}
            \begin{pgfonlayer}{foreground}
                {% for annotation in image.annotations.all %}
                \pgfmathsetmacro{\x}{0.8 * {{ annotation.x_coordinate }} / 100}
                \pgfmathsetmacro{\y}{0.5 * {{ annotation.y_coordinate }} / 100}
                \node[draw, circle, red, thick, minimum size=20pt, inner sep=0pt] at ({\x\textwidth}, -{\y\textwidth}) { {{ forloop.counter }} };
                {% endfor %}
            \end{pgfonlayer}
        \end{tikzpicture}
        \caption{ {{ image.comment }} }
    \end{figure}
        {% for annotation in image.annotations.all %}
        Annotation {{ forloop.counter }}: {{ annotation.annotation_text }}
        {% if annotation.annotation_image %}
        \begin{figure}
            \centering
            \includegraphics[width=0.2\textwidth]{ {{ annotation.annotation_image.path|escape }} }
            \caption{Annotation Image {{ forloop.counter }}}
        \end{figure}
        {% endif %}
        {% endfor %}
    {% endfor %}
\end{itemize}